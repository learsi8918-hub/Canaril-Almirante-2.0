import 'package:flutter/material.dart';
import '../core/theme_controller.dart';
import '../services/export_service.dart';
import '../database/db_helper.dart';
class SettingsScreen extends StatelessWidget{final AppThemeController theme;const SettingsScreen({super.key,required this.theme});Future<void>_export(BuildContext c)async{await ExportService.shareBackup();} @override Widget build(BuildContext c)=>Scaffold(appBar:AppBar(title:const Text('Configurações')),body:ListView(padding:const EdgeInsets.all(12),children:[Card(child:ListTile(leading:const Icon(Icons.business),title:Text(theme.houseName),subtitle:Text('${theme.breederName}\n${theme.city}-${theme.state} · Clube: ${theme.club} ${theme.clubCode}'))),Card(child:Column(children:[const ListTile(title:Text('Identidade visual'),subtitle:Text('As cores atuais foram extraídas automaticamente da logo.')),ListTile(leading:Icon(Icons.palette,color:theme.primary),title:const Text('Cor principal'),subtitle:Text('Derivada da logo')),ListTile(leading:Icon(Icons.circle,color:theme.secondary),title:const Text('Cor secundária'),subtitle:Text('Derivada da logo'))])),Card(child:Column(children:[ListTile(leading:const Icon(Icons.backup),title:const Text('Backup dos dados'),subtitle:const Text('Exporta aves, reprodução, ovos, filhotes, saúde e histórico.'),onTap:()=>_export(c)),ListTile(leading:const Icon(Icons.storage),title:const Text('Banco de dados'),subtitle:const Text('SQLite local como fonte principal dos dados.'))])),Card(child:Column(children:[const ListTile(title:Text('Módulos V14')),ListTile(leading:const Icon(Icons.flutter_dash),title:const Text('Plantel'),subtitle:const Text('Anilha, gaiola, sexo, FOB, origem, foto e observações')),ListTile(leading:const Icon(Icons.favorite),title:const Text('Reprodução'),subtitle:const Text('Monogamia, bigamia, poligamia, ovos e datas')),ListTile(leading:const Icon(Icons.account_tree),title:const Text('Genealogia'),subtitle:const Text('Pais e descendentes preservados')),ListTile(leading:const Icon(Icons.medical_services),title:const Text('Saúde'),subtitle:const Text('Tratamentos, medicamentos, dosagem, quarentena e histórico')),ListTile(leading:const Icon(Icons.sync_alt),title:const Text('Retrocruzamento'),subtitle:const Text('Planejamento teórico por gerações, sem inventar genótipos desconhecidos'))]))]));}}
